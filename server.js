const square = num =  Number(num.toString().split('').map(e=>e*e).join(''))

function square(num){
    let numb = num.toString()
    let sqr = []

    for(let i=0;i< numb.length;i++){
        sqr.push(numb[i]*numb[i])
    }

    return Number(sqr.join(''))

}

remainder = int % dispatch







const square = (num) => Number(num.toString().split('').map(e=>e*e).join(''))
















